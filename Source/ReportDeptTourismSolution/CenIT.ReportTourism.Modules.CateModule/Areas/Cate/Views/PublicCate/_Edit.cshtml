@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Edit"), AppProcessor.Messagor.GetMessage("PublicCate_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    bg-primary
}

@section FormIcon{
    <i class="fa fa-pencil"></i>
}

@section FormBody{
    @using (Html.BeginForm("Edit", "PublicCate", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "EditPublicCate", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_PublicCate")
        </div>
    }
    <script type="text/javascript">
        $('form').ajaxForm({
            success: function(response) {
                if (response.status != undefined) {
                    $("#ModalContent #modal_EditPublicCate").modal("hide");
                    $("#ModalContent #modal_EditPublicCate").on('hidden.bs.modal',
                        function() {
                            if (response.status != undefined) {
                                eval(response.message);
                                _tablePublicCate.ajax.reload(null, null);
                                response.status = undefined;
                            }
                        });
                } else {
                    $("#ModalContent #modal_EditPublicCate" + " #bodyForm").html(response);
                }
            }
        });
    </script>
}