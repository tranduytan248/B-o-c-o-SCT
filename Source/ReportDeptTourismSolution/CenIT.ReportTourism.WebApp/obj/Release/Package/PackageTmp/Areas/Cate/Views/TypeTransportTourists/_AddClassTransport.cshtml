@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("Enterprise_TransportTourists_Infrastructure_Room_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    bg-primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{
    @using (Html.BeginForm("AddClassTransport", "TypeTransportTourists", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "AddClassTransport", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ClassTransport")
        </div>
    }
    <script type="text/javascript">
        $('form').ajaxForm({
            success: function(response) {
                if (response.status != undefined) {
                    $("#ModalContent #modal_AddClassTransport").modal("hide");
                    $("#ModalContent #modal_AddClassTransport").on('hidden.bs.modal',
                        function() {
                            if (response.status != undefined) {
                                eval(response.message);
                                _tableClassTransport.ajax.reload(null, false);
                                response.status = undefined;
                            }
                        });
                } else {
                    $("#ModalContent #modal_AddClassTransport" + " #bodyForm").html(response);
                }
            }
        });
    </script>
}