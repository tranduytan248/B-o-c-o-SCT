@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("Enterprise_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    bg-primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{
    @using (Html.BeginForm("Add", "Enterprise", new { area = "Cate" }, FormMethod.Post, new { encType = "multipart/form-data", id = "AddEnterprise", @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_Enterprise")
        </div>
    }
    <script type="text/javascript">
        $('form').ajaxForm({
            success: function(response) {
                if (response.status != undefined) {
                    $("#ModalContent #modal_AddEnterprise").modal("hide");
                    $("#ModalContent #modal_AddEnterprise").on('hidden.bs.modal',
                        function() {
                            if (response.status != undefined) {
                                eval(response.message);
                                _tableEnterprise.ajax.reload(null, false);
                                response.status = undefined;
                            }
                        });
                } else {
                    $("#ModalContent #modal_AddEnterprise" + " #bodyForm").html(response);
                }
            }
        });
    </script>
}