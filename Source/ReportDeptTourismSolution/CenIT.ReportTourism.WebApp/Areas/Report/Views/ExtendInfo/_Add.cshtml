@using TSFramework.App.Processors
@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Add"), AppProcessor.Messagor.GetMessage("ExtendInfo_Title"));
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType{
    bg-primary
}

@section FormIcon{
    <i class="fa fa-plus"></i>
}

@section FormBody{

    @using (Ajax.BeginForm("Add", "ExtendInfo", new { area = "Report" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "ExtendInfo_OnProcessSuccess(data, 'AddExtendInfo')"
    }, new { @class = "form-horizontal" }))
    {
        <div id="bodyForm">
            @Html.Partial("_ExtendInfos")
        </div>
    }
}