@using TSFramework.App.Processors
@model CenIT.ReportTourism.Models.Cate.CateProvinceModel

@{
    ViewBag.Title = string.Format(AppProcessor.Messagor.GetMessage("Modal_Title_Delete"), AppProcessor.Messagor.GetMessage("Province_Title"));
    Layout = "~/Views/Shared/_FormDel.cshtml";
}

@section FormBody{
    @using (Ajax.BeginForm("Delete", "Provinces", new { area = "Cate" }, new AjaxOptions
    {
        HttpMethod = "POST",
        OnSuccess = "Street_OnDeleteSuccess(data, 'Delete')"
    }, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ProvinceId)
        <h4> @Html.Raw(ViewBag.ConfirmMessage)</h4>
    }
}